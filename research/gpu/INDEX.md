# GPU research — index

| File | Status | Impact | Summary |
|------|--------|--------|---------|
| [density_opened_two_regime_2026_06_22.md](density_opened_two_regime_2026_06_22.md) | REJECTED (constraint) | — | **Template-constants hypothesis tested on multiZn.** Biggest kernel = `gpu_compute_density_opened<float,false,false>` (53% GPU, 140.8 ms, 71 reg). **Two regimes**: large groups (grid ~5432, 4.85 waves/SM) = 83% of time, pinned at register-limited 58.3% occ + latency-bound (50% scoreboard stall on `tex2D` RDM fetch); small groups (grid ~327, 0.29 waves/SM) = 17%, grid-starved. **No constants to template**: `m`/`points` vary per launch; `lda`/`single_pointer` already templated; 71 reg = dual-spin×dual-pointer GGA accumulator set (algorithmic). 64 reg needs FP-reordering de-unroll/spin-split. **Hidden anyway**: total GPU ~265 ms / 21.5 s wall (~1.2%); pole is CPU-BLAS (BChange 2.89s, diag 3.09s, DIIS commut 0.92s). Win only on SM-starved/old GPUs. |
| [density_opened_launch_bounds_2026_06_21.md](density_opened_launch_bounds_2026_06_21.md) | DONE | MED | Open-shell `gpu_compute_density_opened` had **no `__launch_bounds__`** (closed twin always had `(64,16)`) → 78 reg, 12 blocks/SM. Added `__launch_bounds__(DENSITY_BLOCK_SIZE, 14)` → 71 reg, 14 blocks/SM, **0 spills**. Heavy launches (grid 706+, group_m 129–256, register-bound): achieved warps **18%→36%**, SM throughput **22%→38%**, density kernel total **157.6→140.6 ms (−10.8%)**. `(64,16)` rejected — spills (64-reg target, open does 2× α+β work). Bit-exact (launch_bounds is non-arithmetic; 02_Fe3H2O6 open-shell guard PASS). multiZn wall unchanged (CPU-BLAS bound) — win lands where g2g is the pole (older/SM-starved GPUs, large systems). |
| [density_lds128_shared_tiles_2026_06_10.md](density_lds128_shared_tiles_2026_06_10.md) | DONE (+2 dead ends) | HIGH | Density kernels were **LSU-bound**: float4 tiles → LDS.128, fj packed in `.w`, warp-shuffle epilogue (opened α/β reduce concurrently). All bit-exact. Kernel 283→175 ms (−38%), wall −7%, g2g 10.4 ms/iter hides under int3lu. **Pass-3 REJECTED with data: tile double-buffering (+17% — occupancy-sensitive, don't trade shared for barriers) and unroll-8 (flat, reg-capped). m≤64 groups only 6.7% → closed single_pointer not worth it. Kernel at practical local optimum for fixed geometry.** |
| [rho_ls_density_reuse_2026_06_01.md](rho_ls_density_reuse_2026_06_01.md) | OPEN | HIGH | Rho linear search (`11_LinearSearchRho`) is 88% of wall; **94% of its GPU time is recomputing grid-point density from the density matrix**, only 4% is the XC functional. The 726 energy evals all blend two *fixed* endpoint density matrices `(1−λ)P0+λP1`, and density is linear in P → precompute endpoint densities once/step, blend pointwise, reuse across all λ/cycles (~5× on cycle-heavy steps). Designed + profiled; validate by OMP-median iters + basin energy (case is heme-class chaotic). **Energy-floor LS-skip gate REJECTED (53→82 iters).** |
| [partition_geometry_megakernel_evaluation_2026_05_31.md](partition_geometry_megakernel_evaluation_2026_05_31.md) | REJECTED (scoped) | — | Bigger groups / single mega-launch / geometry change on heme. Measured: g2g (94.5ms) is overlapped with int3lu (84.1ms, idle 10.3ms) ⇒ g2g wall ceiling ~4%; GPU is ~100% busy during g2g (Σkernel-time/8 solves ≈ 95ms ≈ measured wall) ⇒ no starvation to fill; launch overhead lives in the 0.5% small-group slice. All three proposals reject (≤4% reward, unreachable, + float32 trajectory risk). **Caveat:** on GTX-1080-class GPUs / large systems g2g becomes the binding pole — but the lever there is density-kernel efficiency, not partition geometry. Real exposed heme levers are CPU/BLAS: diag 28%, DIIS/base-change setup 21%. |
| [tensor_core_feasibility_2026_05_31.md](tensor_core_feasibility_2026_05_31.md) | REJECTED (boundary documented) | — | Ampere TF32/FP16 tensor cores for the density/Fock path. Measured: TF32 in the density contraction ~2×'s heme iters + shifts energy ~250 µHa out of basin; tensor-core GEMM gives only 1.2–1.5× (TF32)/1.6–1.9× (FP16) at heme's small K=group_m shapes; density wall-payoff capped ~5% by int3lu overlap. Precision-safe path (3×TF32) nets <1×. Lists conditions where it *would* pay off (large/benign/non-iterated). |
| [density_opened_point_batch_dead_end_2026_05_25.md](density_opened_point_batch_dead_end_2026_05_25.md) | REJECTED | — | Point-batching (P=4) and `__launch_bounds__` on `gpu_compute_density_opened`; net-neutral, reg-pressure-limited. |
| tc_gemm_probe.cu | TOOL | — | cuBLAS FP32 vs TF32 vs FP16 GEMM ceiling microbench at density-GEMM shapes. |
