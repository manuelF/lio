# CPU research — index

| File | Status | Impact | Summary |
|------|--------|--------|---------|
| [int3G_openmp_2026_06_18.md](int3G_openmp_2026_06_18.md) | DONE | MED-HIGH | `int3G` (3-center Coulomb force gradient) was serial — 2nd-largest force cost. Parallelized over the outer shell loop of all 18 shell-blocks via a contained worker (auto-private scratch incl. the previously-shared `Q`/`W`) + per-thread frc slots (deterministic). **Coulomb gradients 1.32→0.19 s (~7×), fosfato total 9.28→8.13 s**; e2e PASS. Cumulative 2026-06-18 forces work: fosfato 12.27→8.13 s (−34%). Forces now 17% of wall (was 48%) → SCF XC solve is the pole again. |
| [intsolG_openmp_2026_06_18.md](intsolG_openmp_2026_06_18.md) | DONE | HIGH | `intsolG` (QM/MM solvent gradients) was **fully serial** and the single largest cost in fosfato (3.97 s = 32% of total wall, 68% of Forces). Parallelized over the MM-atom range: frc_mm rows written by disjoint threads (**bit-exact**), QM forces summed in fixed thread order (deterministic), all integral scratch moved into a contained worker so it's auto-private (no manual `private(...)` race risk). **QM/MM gradients 3.97→0.65 s (~6× on 8 cores), fosfato total 12.27→9.28 s (−24%)**; all e2e PASS. Next: `int3G` Coulomb gradients (1.32 s), same structure. |
| [scf_diag_at_lapack_floor_2026_06_16.md](scf_diag_at_lapack_floor_2026_06_16.md) | DEAD | REF | SCF Fock `diag` (7 ms, ~30% of fosfato iter) is at the OpenBLAS D&C floor for M=364. Benchmarked on the real iter-15 ON Fock: DSYEVD 6.4 ms; subset solvers (DSYEVR/DSYEVX 'I', lowest NCO=100) **2× slower** (MRRR/bisection fixed costs + cluster reorthog beat the arithmetic saving); plain QR 5× slower; 2-stage rejects 'V'; single-precision only 1.42× and unsafe (1e-6 eigvec perturbation → heme chaos); LAPACK already openblas-openmp (not netlib); cuSOLVER 2.4× slower; no legal GPU overlap (strict SCF dependency). Only headroom = **fewer diags** (iter-count project) or pseudo-diag near convergence (heme-risky, not pursued). |
| [int3lu_gpu_offload_2026_06_10.md](int3lu_gpu_offload_2026_06_10.md) | DONE | MED (HIGH on large closed-shell) | int3lu's bandwidth-bound cool/cools GEMVs (326 MB/iter on fosfato) moved to GPU-resident cuBLAS with background prefetch + automatic CPU fallback (<16 MB, VRAM, cpu-only). int3lu 12→1.1 ms/iter (overlap), Coulomb fit+Fock −72% (default path). Gate lifted 2026-06-11 with GlobalMemoryPool-coordinated VRAM reservation (20% headroom + pool carve-out): multiZn int3lu 590→68 ms/iter, wall −9.6%; heme distributions overlap (hybrid builds run-to-run nondeterministic). 12_Zn run.sh found commented out (test never ran). |
| [rho_ls_endpoint_reuse_cpu_2026_06_04.md](rho_ls_endpoint_reuse_cpu_2026_06_04.md) | DONE | MED-HIGH | `11_LinearSearchRho` (open-shell Rho_LS): `LS - XC g2g` was 83% of wall — the line search recomputed the grid density at every lambda. Grid density is linear in the density matrix → cache endpoint densities once, blend per lambda, run only the functional. `LS - XC g2g` 42.5s→10.9s (~4×); median wall **1.65–1.84× across OMP={4,6,8}**. **Blend coefficients must be double** (float coeffs → 6e-6 curve error → non-convergence); float endpoints + float functional are fine. Energy always within tol; iter-count chaotic for both baseline and reuse (validate by OMP-median). Closes the OPEN gpu/`rho_ls_density_reuse` lever on the CPU side. |
| [cpu_density_points_batched_2026_06_03.md](cpu_density_points_batched_2026_06_03.md) | DONE | HIGH | Two CPU-only g2g wins. **(1) Density rewrite:** `cpu_compute_density_gga` (60.7% of wall) ran *scalar* (float reduction, no `-ffast-math`); reformulated points-batched `cpu_compute_density_gga_batch` — axpy over points (vectorizes, no reassociation), **point-tiled B=8** to keep `rmm` L1-resident (transposing all points first was 3× *slower*). **(2) Function caching:** enabled the existing `inGlobal` cache for CPU-only (two guard edits: `GPU_KERNELS`→`CPU_RECOMPUTE`), eliminating per-iter `compute_functions` recompute; **bit-exact** (proven at OMP=1: −2148.6508600 cached==reference), invalidation structurally safe (unconditional `regenerate` per grid reload), validated on 5-step Ehrenfest. Cumulative: **g2g 533→120 ms (−77%), wall 22.9→11.5 s (−50%)**. fosfato + Fe3H2O6 + travis + Ehrenfest all PASS. |
